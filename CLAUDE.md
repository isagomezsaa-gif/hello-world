# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project

A static HTML page. No build step, no dependencies, no package manager.

## Development

Open `index.html` directly in a browser to preview — no server required.

## Version Control

All changes should be committed with clear messages and pushed to GitHub (`origin/master`) so every version is saved and recoverable.
